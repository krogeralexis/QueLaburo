RewriteEngine On

# Evita que se reescriban archivos existentes
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Redirige todo a index.php pasando la URL
RewriteRule ^(.*)$ index.php?url=$1 [QSA,L]